import React from 'react';

const Settings = () => {
    return (
        <div className="settings-page" id="settings-page">
            
            
        </div>
    );
};

export default Settings;